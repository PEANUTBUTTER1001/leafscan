# LeafScan Lab GUI Study 실행 모드 — MVP 기획서

> 기준 문서: `06_GUI_Study_실행모드_SRS.md`  
> 작성 방식: MVP 명세 도구 결과를 사용하되, 도구의 ML 템플릿이 GUI 기능을 범위 밖으로 분류한 부분은 원본 SRS와 사용자 의도에 따라 정정했다. 이번 MVP의 대상은 **기존 GUI의 Study 실행 확장**이다.

## 1. 배경과 문제 정의

LeafScan Lab은 현재 GUI에서 한 모델을 학습할 수 있고, 여러 모델을 공정하게 비교하는 기능은 `run_study.py` 명령으로 제공한다. 따라서 GUI 사용자도 비교 실험을 하려면 명령 프롬프트에서 Study 이름, 모델 목록, 옵션을 직접 작성해야 한다.

이 MVP는 기존 단일 실행 화면을 바꾸지 않고, 좌측 사이드바 최상단의 실행 모드 선택으로 단일 학습과 순차 비교 학습을 전환한다. Study 모드는 기존 `run_study.py`를 재사용해 모델을 하나씩 실행하며, 데이터 분할 고정·중단 후 재개·`STUDY.md` 결과표 생성 규칙을 그대로 보장한다.

## 2. 목표와 기대 효과

- 명령어를 외우지 않아도 GUI에서 2개 이상 모델의 비교 학습을 시작한다.
- 단일 실행 사용자는 기존 설정·화면·결과 흐름을 그대로 사용한다.
- 비교 대상 전체에 같은 데이터 분할과 공통 학습 설정을 적용해 결과 해석 오류를 줄인다.
- 긴 순차 학습 동안 현재 모델, 대기 모델, 완료 모델을 GUI에서 확인한다.
- 중단 후 동일 Study 이름으로 다시 실행하면 완료 항목을 건너뛰고 이어서 수행한다.

## 3. 대상 사용자와 사용 시나리오

| 사용자 | 시나리오 | 기대 결과 |
|---|---|---|
| 모델 성능을 비교하려는 실험자 | Study 모드에서 `resnet18`, `mobilenet_v3_small`, `efficientnet_b0`를 체크하고 큐를 시작한다. | 세 모델이 순서대로 학습되고 `STUDY.md` 비교표가 생성된다. |
| 한 모델의 설정을 점검하려는 사용자 | 기본 모드인 단일 실행에서 `arch`와 학습 조건을 선택해 실행한다. | 기존 GUI와 같은 단일 run 결과가 생성된다. |
| 학습을 중간에 멈춘 사용자 | Study 실행을 중단한 뒤 같은 Study 이름으로 재시작한다. | 완료된 모델은 건너뛰고 남은 모델만 수행된다. |

## 4. 핵심 가치 제안

기존 GUI의 익숙한 작업 흐름 안에서 비교 실험의 재현성과 편의성을 함께 제공한다. 별도 학습 엔진을 새로 만들지 않고 검증된 `run_study.py`를 연결하므로, GUI와 명령줄 실행의 결과 규칙이 나뉘지 않는다.

## 5. MVP 범위

| 구분 | 항목 | 이유 |
|---|---|---|
| 포함 | 좌측 사이드바 최상단의 `단일 실행 / Study 실행` 모드 선택 | 진입점이자 핵심 UX |
| 포함 | 단일 실행 UI·동작 보존 | 기존 사용자 회귀 방지 |
| 포함 | Study 이름, 최소 2개 모델 다중 선택, LR 자동 탐색 | Study 실행을 설정하기 위한 최소 입력 |
| 포함 | 단일 `arch`·`simple_cnn` 전용 옵션 비활성화와 안내 | Study 모드의 혼동 및 비교 조건 오염 방지 |
| 포함 | `run_study.py` 재사용, 순차 실행·중단·재개 | 기존 비교 규칙과 결과 형식 유지 |
| 포함 | GUI 로그와 큐 상태 표시 | 장시간 실행의 상태 확인 |
| 포함 | `STUDY.md` 및 개별 run 결과 접근 | 비교 결과 확인 |
| 제외 | 병렬 학습·다중 GPU 실행 | 순차 실행 MVP 범위 밖 |
| 제외 | `run_study.py`의 비교/분할 정책 변경 | 기존 검증 로직 보존 |
| 제외 | 모델 추가·데이터 포맷 변경 | 이번 기능의 목적과 무관 |
| 제외 | 별도 화면·새 상단바·새 GUI 창 | 기존 디자인 보존 원칙 |
| 제외 | 결과 자동 해석·모델 추천 | 비교 결과 생성 이후의 고도화 기능 |

## 6. 성공 지표

| 지표 | 성공 기준 |
|---|---|
| Study 시작 성공률 | 모델을 2개 이상 선택한 유효 요청은 GUI에서 Study 프로세스를 시작할 수 있다. |
| 단일 실행 회귀 | 단일 모드에서 기존 설정 수집·학습 시작·중단·결과 저장 흐름이 유지된다. |
| 비교 공정성 | Study 내 모든 run이 동일 Study의 `split.json`을 사용한다. |
| 재개 동작 | 완료 run이 있는 Study 재시작 시 완료 모델을 다시 학습하지 않는다. |
| 사용성 | Study 모드에서 단일 모델 전용 항목이 비활성 상태와 안내 문구로 식별된다. |

## 7. 리스크와 가정

| 구분 | 내용 | 대응 |
|---|---|---|
| 가정 | 현재 `run_study.py`가 GUI에서 전달한 base config를 그대로 처리한다. | GUI는 CLI 계약을 바꾸지 않고 기존 인자를 생성한다. |
| 리스크 | Study 모드에서 특정 모델 전용 옵션을 사용하면 공정 비교로 오해할 수 있다. | `arch`와 `simple_cnn` 전용 옵션을 회색 비활성화하고 이유를 표시한다. |
| 리스크 | 여러 모델 선택 시 총 학습 시간이 길다. | 선택 개수, 순차 실행 방식, LR 탐색의 시간 증가를 표시한다. |
| 리스크 | 하위 워커 로그가 섞여 현재 모델을 파악하기 어렵다. | 현재/대기/완료/실패 상태를 별도 큐 표시로 제공한다. |
| 리스크 | Study 중단 후 데이터나 설정이 달라질 수 있다. | 동일 Study 이름과 기존 `split.json` 재사용 규칙을 명시한다. |
