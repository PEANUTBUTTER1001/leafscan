# LeafScan 팀 협업 규약

> **대상**: 실험을 시작하려는 모든 팀원.
> **왜 필요한가**: 여러 사람이 각자 PC에서 실험을 돌리면, 아무 규칙 없이는 **실험 ID가 충돌하고, 결과를 비교할 수 없고, 같은 실험을 두 번 하게 됩니다.**
> **핵심 3줄**: ① run ID에 내 이름을 넣습니다 ② 데이터·모델 파일은 커밋하지 않습니다 ③ 실험 전에 보드에서 중복을 확인합니다

---

## 1. 협업에서 실제로 생기는 문제

| 문제 | 방치하면 |
|---|---|
| 두 사람이 동시에 `exp_006`을 만듦 | 합칠 때 충돌. 한쪽 결과가 사라질 수 있음 |
| GPU·CUDA·OS가 달라 결과가 미묘하게 다름 | "누구 결과가 맞나"로 시간 소모 |
| 같은 실험을 두 사람이 돌림 | GPU 시간 낭비 |
| 데이터 버전이 다름 | 비교 자체가 무의미해짐 |
| 대용량 파일을 커밋 | 저장소가 수 GB로 불어나 clone이 불가능해짐 |
| 결과를 각자 PC에만 둠 | 팀 전체가 진행 상황을 모름 |

아래 규약은 이 여섯 가지를 막기 위한 것입니다.

---

## 2. run ID 규칙 — **가장 중요**

### 2.1 형식

```
exp_{YYMMDD}_{이니셜}_{순번}
```

| 예 | 의미 |
|---|---|
| `exp_260815_jk_01` | 2026-08-15, jk가 돌린 그날의 1번째 실험 |
| `exp_260815_sy_03` | 같은 날 sy의 3번째 실험 |

| 규칙 | 내용 |
|---|---|
| 이니셜 | 팀 합류 시 배정받습니다. 소문자 2글자 (`jk`, `sy`, `mh`, …) |
| 순번 | 그날 자기 실험의 순번. 2자리 |
| 설정 위치 | 콘솔 최초 실행 시 이니셜을 한 번 입력하면 이후 자동 생성 |

> **왜 날짜와 이름을 넣는가** — 기존 방식(`exp_001`)은 각자 PC에서 로컬 폴더만 보고 번호를 매기므로, 두 사람이 같은 번호를 만드는 것이 **확정적**입니다. 날짜+이니셜을 넣으면 충돌이 구조적으로 불가능해집니다.

### 2.2 study ID

```
study_{번호}_{짧은영문}
```

| 예 | 내용 |
|---|---|
| `study_01_backbone` | 백본 비교 |
| `study_02_class_imbalance` | 클래스 불균형 대응 |

study는 **팀 공용**입니다. 새 study를 만들 때는 팀 채널에 알립니다.

---

## 3. Git 규칙

### 3.1 커밋하는 것 / 하지 않는 것

| | 커밋 | 이유 |
|---|---|---|
| 코드 (`*.py`) | ✅ | — |
| 문서 (`*.md`) | ✅ | — |
| `configs/*.json` | ✅ | 기준 설정 |
| `runs/*/config.json` | ✅ | 실험의 원본 (작음) |
| `runs/*/metrics.json` | ✅ | 지표 (작음) |
| `runs/*/report.md` | ✅ | 연구 기록 |
| `runs/*/figures/*.png` | ✅ | 시각 자료 (수백 KB) |
| `runs/*/env.json`·`diff.json`·`status.json` | ✅ | 작음 |
| **`runs/*/best.pt`** | ❌ | 수십~수백 MB |
| **`runs/*/*.npy`** | ❌ | 수 MB~수십 MB |
| **`runs/*/train.log`** | ❌ | 길고 재생성 가능 |
| **`data/`** | ❌ | 수 GB |
| `.venv/`, `__pycache__/`, `mlruns/` | ❌ | — |

`.gitignore`에 이미 반영돼 있습니다. **직접 추가하지 말 것.**

```gitignore
data/
.venv/
__pycache__/
mlruns/
runs/*/best.pt
runs/*/*.npy
runs/*/train.log
bundles/*/model.*
```

> **큰 파일을 실수로 커밋했다면** 푸시하기 전에 팀에 알립니다. 이미 푸시됐다면 히스토리에서 제거해야 하므로 혼자 처리하지 말 것.

### 3.2 브랜치

| 상황 | 브랜치 |
|---|---|
| 실험 결과만 추가 | `runs/{이니셜}-{날짜}` → PR |
| 코드 수정 | `feat/{내용}` 또는 `fix/{내용}` → PR |
| 문서 수정 | `docs/{내용}` → PR |

**`main`에 직접 푸시하지 않습니다.** 실험 결과 PR은 리뷰 없이 머지해도 되지만, 코드 PR은 최소 1명이 확인합니다.

### 3.3 실험 결과 올리기

```bash
git checkout -b runs/jk-260815
git add runs/exp_260815_jk_*/ runs/index.csv
git status                      # 큰 파일이 섞이지 않았는지 확인
git commit -m "runs: study_01 resnet50 3건 (lr 탐색)"
git push -u origin runs/jk-260815
```

> `git status`로 **반드시 확인**할 것. `.gitignore`가 있어도 강제 추가하면 들어갑니다.

---

## 4. 데이터 공유

### 4.1 데이터는 각자 직접 내려받습니다

**원본 데이터는 팀 내에서 공유·재배포하지 않습니다.** 각 팀원이 AI 허브에서 자기 계정으로 직접 내려받아 `LeafScan/data/` 아래에 배치합니다.

| 항목 | 방법 | Git |
|---|---|---|
| 원본 tar (작물별) | **각자 AI 허브에서 직접 다운로드** | ❌ |
| `data/_extracted/` | 각자 인제스트로 생성 | ❌ |
| `data/index.csv` | 각자 생성 | ❌ |
| `data/MANIFEST.md` | 인제스트가 생성 | ✅ **커밋** — 버전 대조용 |

### 4.1.1 배치 규칙 — 받은 그대로 유지

```
LeafScan/data/{작물영문}/{training|validation}/{labeled|source}/*.tar
```

| 항목 | 규칙 |
|---|---|
| 폴더명 | `lettuce` · `chard` · `kale` · `leaf_mustard` |
| 파일명 | **원본 그대로.** 바꾸지 않습니다 — 끝 번호가 labeled↔source의 짝 키입니다 |
| 압축 해제 | **하지 않습니다.** 인제스트가 알아서 추출합니다 |
| 짝 확인 | 별도로 하지 않아도 됩니다. 인제스트가 검증하고 불일치 시 중단합니다 |

### 4.1.2 인제스트는 각자 실행

```bash
python scripts/prepare_dataset.py --data-root data --out data/index.csv
```

| 이유 | 설명 |
|---|---|
| 원본을 공유하지 않습니다 | 각자 받은 데이터로 각자 만듭니다 |
| 경로가 PC마다 다릅니다 | `index.csv`의 경로는 각자 환경 기준 |
| 검증이 각자 필요 | 받은 tar가 온전한지 각자 확인해야 합니다 |

> `index.csv`의 **해시는 동일해야 합니다** (경로 제외한 내용 기준). 다르면 받은 아카이브 구성이 다르다는 뜻이며, **그 상태의 실험 결과는 서로 비교할 수 없습니다.**

### 4.1.3 데이터 공개 범위

원본을 공유하지 않더라도, **결과물에 원본 이미지가 섞여 나가는 경로가 두 개 있습니다.**

| 산출물 | 내용 |
|---|---|
| `figures/misclassified_grid.png` | 오분류 샘플의 실제 이미지 |
| `figures/gradcam_samples.png` | 원본 위에 히트맵을 얹은 이미지 |

저장소를 공개하거나 외부 자료에 실을 계획이 생기면 **이 두 종만 제외**하면 됩니다. `make_figures.py`에 `--public` 옵션을 두어 두 종을 건너뛸 수 있게 구현합니다.

### 4.2 데이터 버전 확인 — 비교의 전제

**같은 데이터로 돌린 실험만 비교할 수 있습니다.** 학습 시작 시 `index.csv`의 해시가 `env.json`에 자동 기록되고, 비교 시 다르면 경고가 뜹니다.

```bash
# 내 데이터가 어느 버전인지 확인
python scripts/check_data.py

[OK] data/index.csv — 12,480행 · sha256:8f3a2c…d91b
[OK] MANIFEST와 일치 (v2, 2026-08-10 배포)
```

불일치하면:

```
[!!] index.csv 해시가 MANIFEST와 다릅니다
     내 파일:   sha256:1122ab…
     MANIFEST: sha256:8f3a2c…
     → 최신 데이터를 다시 받으세요. 이 상태의 실험 결과는 비교에 쓸 수 없습니다.
```

### 4.3 데이터가 갱신되면

| 절차 | 담당 |
|---|---|
| 1. 새 버전을 공유 드라이브에 올림 | 데이터 담당 |
| 2. `MANIFEST.md` 갱신 후 커밋 | 데이터 담당 |
| 3. 팀 채널 공지 | 데이터 담당 |
| 4. 각자 받아서 `check_data.py` 확인 | 전원 |
| 5. **이전 버전 실험은 비교에서 분리** | 전원 |

---

## 5. 실험 분담

### 5.1 중복 방지 — 실험 보드

새 실험을 시작하기 전에 **팀 보드(공유 시트 또는 이슈)** 에서 확인하고 예약합니다.

| study | 항목 | 담당 | 상태 |
|---|---|---|---|
| study_01 | resnet50 | jk | ✅ 완료 |
| study_01 | efficientnet_b0 | sy | 🔄 진행 중 |
| study_01 | convnext_tiny | — | ⬜ 미착수 |
| study_01 | resnet18 | mh | 🔄 진행 중 |
| study_01 | mobilenet_v3_small | — | ⬜ 미착수 |

> **비교 study는 arch 단위로 나누기 가장 좋습니다.** 조건이 코드로 고정되므로 각자 돌려도 결과를 합칠 수 있습니다.

### 5.2 부분 실행

```bash
# 나는 convnext_tiny만 담당
python run_study.py --study study_01_backbone \
  --base configs/base_leafscan.json \
  --only arch=convnext_tiny --lr-search
```

**분할 정보(`split.json`)는 study 폴더에서 공유**되므로, 먼저 시작한 사람이 만든 것을 나머지가 그대로 씁니다. 없으면 생성되고, 있으면 재사용됩니다. → **다른 PC에서 돌려도 같은 데이터 분할**이 보장됩니다.

### 5.3 GPU 사양이 다를 때

| 항목 | 영향 | 대응 |
|---|---|---|
| 정확도 지표 | 거의 없음 (미세한 부동소수점 차이) | **비교 가능** |
| 학습 시간 | 큼 | 시간은 비교하지 않거나 GPU를 함께 표기 |
| 배치 크기 | VRAM에 따라 다름 | **가능하면 맞춥니다.** 불가하면 gradient accumulation으로 실효 배치를 맞춤 |

> 배치 크기는 결과에 영향을 줍니다. 4GB PC와 12GB PC가 각각 16과 64로 돌리면 그건 **arch 비교가 아니라 배치 비교**가 섞인 것입니다. `run_study.py`가 이 경우 경고합니다.

---

## 6. 재현성 규칙

| 항목 | 규칙 |
|---|---|
| seed | 기본 42. 변경 시 config에 남으므로 자유롭게 바꿔도 됨 |
| 환경 기록 | `env.json`에 자동 기록 (torch·CUDA·GPU·OS·커밋 해시) |
| 라이브러리 | `requirements.txt` 버전 고정. **임의로 업그레이드하지 않습니다** |
| 업그레이드가 필요하면 | 팀 합의 후 전원 동시 적용 + `RESEARCH_LOG.md`에 기록 |
| 결정성 | GPU 연산은 완전한 결정성이 보장되지 않음. **같은 seed라도 소수점 뒤가 다를 수 있습니다 — 정상입니다** |

> 결과가 남과 미묘하게 다를 때 `env.json`을 먼저 대조합니다. torch 버전이나 GPU가 다르면 그것이 원인이며, **경향이 같으면 문제 삼지 않습니다.**

---

## 7. 기록 규칙

| 항목 | 규칙 |
|---|---|
| 가설 | **모든 run에 필수.** 비면 실행 시 확인 대화상자 |
| 결론 | 실험 종료 후 **당일 안에** 작성. 3~5줄이면 충분 |
| 태그 | 최소 1개 (`study_01`, `lr-search`, `failed` 등) |
| 부모 run | 이전 실험에서 파생된 경우 지정 → diff가 자동 기록됨 |
| 실패 run | **지우지 않습니다.** 실패도 기록입니다. `failed` 태그와 원인 분류가 남음 |
| STUDY.md | study를 마친 사람이 결론 작성 |

### 7.1 결론을 미루면 생기는 일

`index.csv`와 `RESEARCH_LOG.md`에 `⚠️ 미작성`이 쌓입니다. 일주일 지나면 본인도 그 실험을 왜 했는지 기억하지 못하므로, **당일 작성을 원칙**으로 합니다.

---

## 8. 커뮤니케이션

| 상황 | 알릴 곳 | 내용 |
|---|---|---|
| 새 study 시작 | 팀 채널 + 보드 | 질문·담당·예상 run 수 |
| 실험 예약 | 보드 | 중복 방지 |
| study 완료 | 팀 채널 | `STUDY.md` 링크 + 한 줄 결론 |
| 데이터 갱신 | 팀 채널 | 버전·변경점·해시 |
| 라이브러리 변경 제안 | 팀 채널 | 이유·영향 범위 |
| 큰 파일 실수 커밋 | 팀 채널 **즉시** | 푸시 전이면 되돌리기 쉬움 |
| 하네스 버그 발견 | 이슈 | 재현 방법 + `train.log` |

---

## 9. 신규 합류자 체크리스트

| # | 항목 | 문서 |
|---|---|---|
| 1 | 이니셜 배정받기 | 팀 채널 |
| 2 | 설치 완료 (`scripts/check_env.py` 통과) | `README.md` §2 |
| 3 | 더미 데이터로 학습 1회 성공 | `README.md` §3 |
| 4 | 데이터 받고 `check_data.py` 통과 | `README.md` §4 |
| 5 | 이 문서(협업 규약) 정독 | — |
| 6 | 보드에서 담당 실험 확인 | 팀 보드 |
| 7 | 첫 실험 → 결론 작성 → PR | `README.md` §6 |

---

## 10. 요약 카드

```
■ run ID          exp_{YYMMDD}_{이니셜}_{순번}
■ 커밋 O          코드 · 문서 · config · metrics · report · figures
■ 커밋 X          data/ · *.pt · *.npy · train.log · .venv
■ 실험 전         보드에서 중복 확인 → 예약
■ 실행 전         가설 작성 (필수)
■ 실행 후         결론 작성 (당일)
■ 결과 공유       브랜치 runs/{이니셜}-{날짜} → PR
■ 데이터 확인     python scripts/check_data.py
■ 환경 확인       python scripts/check_env.py
■ 실패 run        지우지 않는다
```
