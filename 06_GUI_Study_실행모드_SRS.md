# 요구사항 명세서: LeafScan Lab GUI Study 실행 모드

## 1. 작업 개요

- 작업명: GUI 단일 실행 / Study 실행 모드 확장
- 목적: 현재 `lab_gui.py`의 단일 모델 학습 UI와 디자인을 보존하면서, 여러 모델을 순차 학습·비교하는 Study 실행을 GUI에서 설정하고 시작한다.
- 요청 근거: 사용자는 명령줄 `run_study.py` 대신 현재 GUI에서 단일 실행과 Study 실행을 선택해 사용하고자 한다.

## 2. 구현 범위

### 포함

- 좌측 사이드바의 기존 실행 영역 위에 `단일 실행` / `Study 실행` 모드 선택을 추가한다.
- 기본 모드는 `단일 실행`이며, 기존 UI·동작을 변경하지 않는다.
- Study 모드에서 Study 이름, 비교 모델 다중 선택, 모델별 LR 자동 탐색 설정을 제공한다.
- Study 모드에서 선택 모델을 `run_study.py`로 순차 실행한다.
- Study 모드에서 기존 우측 로그·진행·결과 영역을 유지하고, 현재 실행 모델·대기·완료 모델 상태를 표시한다.
- Study 실행 결과의 `STUDY.md` 및 개별 run 결과 폴더를 기존 결과 열기 흐름에서 접근 가능하게 한다.

### 제외

- `run_study.py`의 비교·분할·재개 규칙 자체 변경
- 병렬 학습, 여러 GPU 동시 실행
- 모델 레지스트리 추가·변경
- 새 화면·새 상단바·별도 GUI 창 제작
- 실제 학습 결과의 자동 해석 또는 모델 추천

## 3. 기능 요구사항

### 3.1 공통 UI 원칙

1. 현재 GUI의 상단, 좌측 카드 순서, 우측 로그·그래프·결과 영역 디자인을 유지한다.
2. 모드 선택은 기존 좌측 사이드바의 실행 섹션 위에 작은 라디오 버튼 또는 세그먼트 컨트롤로 배치한다.
3. 기본 시작 상태는 단일 실행이다.
4. 모드를 바꿔도 공통 학습 설정 값은 유지한다.

### 3.2 단일 실행 모드

1. 현재 `start_training()`과 `train_worker.py`를 호출하는 동작을 유지한다.
2. 단일 `arch` 콤보박스와 `simple_cnn` 전용 구조 파라미터는 활성 상태다.
3. 기존 가설 입력란과 실행 버튼을 유지한다.
4. 실행 버튼 문구는 현재 단일 실행용 문구를 유지한다.

### 3.3 Study 실행 모드

1. Study 이름 입력란을 제공하며 기본값은 `study_01_backbone`으로 둔다.
2. 등록된 arch 목록을 체크박스로 표시하며 최소 2개를 선택해야 실행할 수 있다.
3. 현재 등록된 모델은 최대 6개까지 선택 가능하다. 별도의 임의 상한은 두지 않는다.
4. `simple_cnn`은 선택할 수 있으나, 실험용 fixture 성격임을 설명 문구로 표시한다.
5. `모델별 LR 자동 탐색` 체크박스를 제공하며, 선택 시 `run_study.py --lr-search`를 전달한다.
6. 실행 버튼은 `Study 큐 시작`으로 바뀐다.
7. 선택 모델을 `--vary arch=...` 인자로 전달해 하나의 `run_study.py` 프로세스로 순차 실행한다.
8. `run_study.py`가 생성·재사용하는 study 단위 `split.json`을 그대로 사용해 동일 분할을 보장한다.
9. 이미 `metrics.json`이 존재하는 완료 모델은 `run_study.py`의 기존 동작대로 건너뛴다.

### 3.4 Study 모드의 비활성 UI

아래 단일 모델 전용 항목은 Study 모드에서 회색 비활성화하고, 왜 사용할 수 없는지 알 수 있게 설명 또는 툴팁을 제공한다.

| 항목 | Study 모드 상태 | 안내 문구 |
|---|---|---|
| 단일 `arch` 콤보박스 | 비활성화 | `Study 모드에서는 비교 모델 목록으로 모델을 선택합니다.` |
| `simple_cnn` 전용 구조 파라미터 | 비활성화 | `특정 모델 전용 설정은 공정 비교에 사용하지 않습니다.` |
| 단일 실행용 가설 입력 | 비활성화 또는 숨김 | `Study 이름과 결과표가 실험 묶음을 식별합니다.` |

다음 항목은 모든 선택 모델에 공통으로 적용하므로 Study 모드에서도 활성 상태를 유지한다.

| 공통으로 활성인 항목 |
|---|
| 데이터셋, subset, 검증 비율, split policy, crop mode, ambiguous policy, augmentation |
| 입력 해상도, pretrained, freeze epoch |
| epoch, batch size, LR, optimizer, early stopping, class weight, head 가중치, seed |

### 3.5 LR 규칙

1. LR 자동 탐색이 꺼져 있으면 GUI의 공통 LR을 모든 모델에 적용한다.
2. LR 자동 탐색이 켜져 있으면 모델별 추천 LR 후보를 탐색하고 선택된 LR로 본 학습을 진행한다.
3. LR 자동 탐색이 실행 시간을 늘린다는 안내를 표시한다.

### 3.6 진행 상태 및 중단

1. 기존 우측 로그 출력은 유지한다.
2. Study 모드에서는 현재 모델명, 전체 선택 수, 각 모델의 `완료 / 실행 중 / 대기 / 실패 / 건너뜀` 상태를 표시한다.
3. 기존 중단 버튼은 Study 프로세스도 중단할 수 있어야 한다.
4. 중단 후 같은 Study 이름으로 다시 시작하면 완료된 run은 건너뛰고 남은 run을 실행한다.

## 4. 입력·출력 및 인터페이스

### 입력

- GUI 공통 설정값: 기존 `lab_gui.py`의 config 구성값
- Study 이름: 문자열
- 비교 모델: `ARCHS` 기반의 선택된 모델명 목록
- LR 자동 탐색: Boolean

### 출력

- 단일 실행: 기존 `runs/<run-id>/` 산출물
- Study 실행: `runs/<study-name>/`
  - `split.json`
  - 모델별 run 디렉터리 및 `metrics.json`, `best.pt`
  - `STUDY.md`
- GUI 로그: `run_study.py` 및 하위 `train_worker.py`의 표준 출력

### 기존 계약 영향

- 데이터 포맷, 모델 출력, `train_worker.py`, `run_study.py` CLI 계약은 변경하지 않는다.
- GUI는 Study용 base config JSON을 임시 또는 study 결과 경로에 생성해 `run_study.py --base`에 전달한다.

## 5. 예외 및 경계 상황

| 상황 | 처리 |
|---|---|
| Study 모델을 0개 또는 1개 선택 | 시작을 막고 `Study 실행에는 모델을 2개 이상 선택하세요.` 표시 |
| Study 이름이 비어 있음 | 시작을 막고 이름 입력 요청 |
| 실행 중 설정 변경 | 기존 실행 보호 정책과 동일하게 관련 컨트롤 비활성화 |
| 선택 모델 중 실패 | 실패 상태·로그를 표시하고, `run_study.py`의 기존 정책대로 다음 모델을 계속 시도 |
| 이미 끝난 Study 재시작 | 완료된 모델을 건너뛰고 `STUDY.md`를 다시 생성 |
| 단일 실행으로 모드 전환 | Study 선택 목록은 보존하되 단일 실행에 영향을 주지 않음 |

## 6. 비기능 요구사항 및 제약

- 기존 단일 실행의 UX·동작 회귀가 없어야 한다.
- Study 실행은 병렬이 아닌 순차 실행이다.
- 공정 비교를 위해 선택된 모델은 같은 데이터 분할과 공통 학습 설정을 사용한다.
- Study 전용 UI는 기존 Tkinter/ttk 스타일과 색상 체계를 따른다.
- 변경은 가급적 `lab_gui.py`에 국한하고, 공통 로직은 기존 `run_study.py`를 재사용한다.

## 7. 가정 및 확인 필요 사항

### 가정

- Study 모드의 `pretrained`, `freeze epoch`, `img_size`, `epoch`, `batch size`는 선택한 모든 모델에 같은 값으로 적용한다.
- Study 결과의 기준 이름은 사용자가 입력한 Study 이름이며, 같은 이름은 재개 목적의 실행으로 해석한다.
- 별도 모드 전용 화면은 만들지 않고 현재 창의 조건부 표시·비활성화만 사용한다.

### 확인 필요

- 없음. 위 항목을 승인하면 구현을 시작할 수 있다.

## 8. 수용 기준

- [ ] GUI 시작 시 단일 실행이 기본 선택되고, 기존 단일 실행 흐름이 동일하게 동작한다.
- [ ] Study 모드 선택 시 단일 arch와 simple_cnn 전용 설정이 회색 비활성화되며 이유를 확인할 수 있다.
- [ ] Study 모드에서 2개 이상의 모델과 Study 이름을 설정할 수 있다.
- [ ] 0개 또는 1개 선택 상태에서는 Study 실행이 시작되지 않는다.
- [ ] Study 시작 시 선택 모델이 지정 순서로 `run_study.py`를 통해 순차 실행된다.
- [ ] Study 실행 중 현재·대기·완료 모델 상태와 로그를 GUI에서 확인할 수 있다.
- [ ] Study 완료 시 기존 study 산출물과 `STUDY.md`가 생성된다.
- [ ] 중단 후 같은 Study 이름으로 재실행하면 완료 run이 건너뛰어진다.

## 9. 최소 변경 구현 계획

1. `lab_gui.py`의 상태 변수에 실행 모드, Study 이름, 선택 모델, LR 탐색 여부를 추가한다.
2. 기존 좌측 실행 영역 위에 모드 선택 컨트롤을 추가하고, Study 전용 설정 프레임을 조건부로 표시한다.
3. 모드 변경 핸들러로 단일 모델 전용 컨트롤의 활성/비활성 상태와 실행 버튼 문구를 전환한다.
4. 현재 GUI config 수집 로직을 재사용해 Study용 base config를 만들고, `run_study.py` 프로세스 실행 경로를 추가한다.
5. 기존 워커 메시지·프로세스 관리 방식을 확장해 Study 로그와 큐 상태를 표시하고 중단을 지원한다.
6. 단일 실행 회귀와 Study 유효성 검사를 추가하고 수동 GUI 시나리오로 검증한다.

## 10. 검증 계획

- 단일 실행: 기존 preset으로 학습 시작, 로그 수신, 중단, 결과 저장을 확인한다.
- Study 유효성: 0개·1개 모델 선택에서 실행이 막히는지 확인한다.
- Study 정상: 2개 이상의 모델을 선택하고, 생성된 base config·명령 인자·Study 폴더·`STUDY.md`를 확인한다.
- 상태 전환: 단일 ↔ Study 모드 전환 시 활성/비활성 대상과 값 보존을 확인한다.
- 재개: 완료 run이 있는 Study를 다시 시작해 해당 run이 건너뛰어지는지 확인한다.

## 승인 요청

명세가 맞으면 `명세 확정, 구현 시작`이라고 말씀해 주세요.
