# GPU(CUDA) 우선 설치 — torch/torchvision은 PyPI 기본 인덱스가 아니라
# CUDA 전용 인덱스에서 받아야 GPU 빌드가 잡힌다. 아래를 requirements.txt 설치보다 먼저 실행할 것.
#
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu132
#
# 설치 후 확인 (GTX 1650 기준 검증됨 — torch 2.13.0+cu132, cuda available True):
#   python -c "import torch; print(torch.__version__); print(torch.cuda.is_available()); print(torch.cuda.get_device_name(0) if torch.cuda.is_available() else 'GPU 미인식')"
#
# GPU가 없거나 위 설치를 생략해도 CPU 빌드로 정상 동작한다 (device: auto 가 자동 판별).

torch>=2.13.0
torchvision>=0.28.0
numpy
scikit-learn
matplotlib
pillow

# 선택 — 있으면 실험 추적에 사용, 없어도 학습은 정상 완주 (P8)
# mlflow
# wandb
