# 전처리 진행 표시 · 아카이브 번호 공백 · GUI 기본값 초기화 — 구현 계획

> 작성일: 2026-08-13
> 대상 이슈 3건. 파일이 분리되어 서로 영향이 없다.

| 이슈 | 내용 | 수정 파일 |
|---|---|---|
| A | `prepare_dataset.py` 인덱스 생성 진행 상태 표시 | `scripts/prepare_dataset.py` |
| B | 아카이브 번호 공백을 오류로 취급하지 않도록 안내 기준 정리 | `scripts/prepare_dataset.py` |
| C | GUI 기본값 초기화 버튼 | `lab_gui.py` |
| 공통 | 테스트 | `tests/test_prepare_dataset.py`, GUI 스모크 |

공통 원칙: **데이터 처리 규칙·산출물·학습 동작은 변경하지 않는다.** 출력·안내·UI만 추가한다.

---

## 이슈 A — 인덱스 생성 진행 상태 표시

### A-1. 문제: 정상 동작일수록 출력이 없다

현재 데이터 규모 — tar 174개(87쌍) · 42.3GB · 아카이브당 약 501파일 · 최대 약 43,500장.

| 코드 위치 | 하는 일 | 현재 출력 | 실측/추정 |
|---|---|---|---|
| `discover_archives` | `rglob("*.tar")` | 명명 불일치 경고만 | 빠름 |
| `validate_pairs` | tar 174개를 전부 열어 멤버 목록 스캔 (`_tar_stems` × 2) | **0줄** | labeled 0.03s + source 0.3s × 87 ≈ 30초 (디스크 캐시 없으면 수 분) |
| `extract_archive` | 42.3GB 추출. 재실행 시에도 tar 를 열어 멤버 수 카운트 | **0줄** | 가장 긴 구간 |
| `reconcile_extracted_pair` | 추출본 rglob + 짝 안 맞는 파일 삭제 | 삭제가 있을 때만 | 아카이브당 약 1,000파일 스캔 |
| 아카이브별 stem 루프 | JSON 파싱 + PIL 이미지 검사 × 약 43,500회 | 경고가 있을 때만 = **정상이면 0줄** | 두 번째로 긴 구간 |
| `write_manifest` → `_sha256` | index.csv 전체 해시 | 0줄 | 빠름 |

경고가 없는 깨끗한 데이터일수록 완전히 멈춘 것처럼 보인다.

### A-2. 설계 방침

- 처리 순서·로직·산출물은 한 줄도 바꾸지 않는다. 출력만 추가한다.
- 요구사항의 7단계 중 **③추출 · ④매칭 · ⑤파싱 · ⑥이미지검사는 코드상 아카이브별로 인터리브**되어 있다(하나의 `for pair` 루프). 전역 단계로 분리하면 처리 순서와 메모리 사용이 바뀌므로 재구조화하지 않고, "아카이브별 처리" 단계의 하위 단계로 표시한다.
- 진행률 갱신은 **시간 기반 스로틀**(기본 0.5초). 43,500줄 폭주 방지.
- **TTY / 비TTY 분기**

| | 터미널 (TTY) | 파일·파이프 (비TTY) |
|---|---|---|
| 줄 갱신 | `\r` 로 제자리 덮어쓰기 | `\r` 없이 새 줄 |
| 갱신 주기 | 0.5초 | 10초 또는 10% 단위 |
| 스피너 `- \ \| /` | 표시 | 표시 안 함 |

  `\r` 은 파일 안에서 줄을 바꾸지 않아, 리다이렉트 시 수천 번의 갱신이 한 줄에 뭉쳐 로그가 못 쓰게 된다.

- **경고와 진행 줄 충돌 방지**: 경고를 찍기 전에 진행 줄을 지우고, 출력 후 다시 그린다. 기존 `log()` 를 이 규칙을 타도록 감싸므로 **기존 경고 문구는 그대로** 유지된다.
- ASCII + 한글만 사용. 박스문자·이모지 없음. 스피너도 `-\|/`.
- **UTF-8 출력 고정**: 스크립트 시작에서 stdout/stderr 를 UTF-8 로 재설정한다. 현재 코드는 로그 문구에 `—`(em dash)를 쓰는데, Windows 에서 출력을 파일로 리다이렉트하면 시스템 코드페이지(cp949)로 떨어져 `UnicodeEncodeError` 로 죽는다. 진행률 추가로 출력량이 늘면 이 문제를 만날 확률이 크게 높아진다.

  ```python
  if hasattr(sys.stdout, "reconfigure"):
      sys.stdout.reconfigure(encoding="utf-8", errors="replace")
      sys.stderr.reconfigure(encoding="utf-8", errors="replace")
  ```

### A-3. 단계 구성

```
[단계 1/5] 아카이브 탐색
[단계 2/5] labeled/source 짝 검증
[단계 3/5] 아카이브별 처리 (추출 → 매칭 → 파싱 → 이미지 검사)
[단계 4/5] index.csv 저장
[단계 5/5] labels.json · MANIFEST.md 생성
```

### A-4. 출력 예시

```
[단계 1/5] 아카이브 탐색
[진행] 아카이브 174개 발견 (labeled 87 · source 87)

[단계 2/5] labeled/source 짝 검증
[진행] 짝 검증 43 / 87쌍 (49.4%) — TL_3.상추43  \
[진행] 아카이브 검증 완료 — 사용 가능 85쌍 · 번호 공백 0 · 경고 0 · 오류 2

[단계 3/5] 아카이브별 처리 (추출 → 매칭 → 파싱 → 이미지 검사)
[진행] (12/85) TL_3.상추52 추출 중 ...  |
[진행] (12/85) TL_3.상추52 매칭 완료 — 정상 469쌍 (JSON만 0 · JPG만 0 삭제)
[진행] (12/85) TL_3.상추52 처리 중 — 235 / 469건 (50.1%)
       등록 232건 · 제외 3건 · 경과 00:04:12 · 남은 예상 00:03:58
[진행] (12/85) TL_3.상추52 처리 완료 — 469건 등록 · 누적 5,631건

[단계 4/5] index.csv 저장 중...
[단계 5/5] labels.json · MANIFEST.md 생성

[완료] index.csv 생성 — 12,480장 · 제외 31장 · 총 소요 00:41:07
       작물: {...}   단계: {...}   개체(group): 1,240개
       제외: {'image_corrupt': 28, 'json_parse': 3}
```

### A-5. 추가 구조

모두 `scripts/prepare_dataset.py` 안. 외부 의존 없음.

```python
class Progress:                 # 시간 스로틀 + TTY 분기 + 스피너
    def __init__(self, total, label, stream=None, clock=None, interval=0.5)
    def update(self, done, extra="")
    def close(self, msg)
def step(n, total, title)       # [단계 n/N] 제목
def fmt_hms(sec)                # 00:04:12
def fmt_eta(elapsed, done, total)
```

`stream` / `clock` 을 주입 가능하게 만들어 테스트에서 시간·출력을 결정론적으로 검증한다.

### A-6. 오류 컨텍스트

현재 `[중단] 미지의 작물 라벨 'X' @ {stem}` 에는 아카이브 정보가 없다. 메시지에 아카이브·단계·진행 상황을 추가한다(종료 코드·동작은 동일).

```
[중단] 미지의 작물 라벨 '무' @ TL_3.상추52 / 파싱 단계 / stem=C26_L01_07_...
       진행 상황: 아카이브 12/85 · 이 아카이브 235/469건 · 누적 등록 5,631건
```

아카이브별 처리 블록을 `try/except` 로 감싸 예상치 못한 예외에도 "어느 아카이브 / 어느 하위 단계"를 찍고 **그대로 re-raise** 한다(예외를 삼키지 않는다).

### A-7. CLI 옵션

| 옵션 | 기본 | 설명 |
|---|---|---|
| `--quiet` | off | 진행률 끄기 (단계 헤더와 최종 요약만) |
| `--progress-interval` | `0.5` | 갱신 주기(초) |
| `--no-spinner` | off | 스피너만 끄기 (비TTY 에서는 자동 off) |

기존 옵션(`--data-root`, `--out`, `--skip-broken`, `--crops`, `--no-verify-images`)은 그대로.

---

## 이슈 B — 아카이브 번호 공백

### B-1. 조사 결과: 현재 코드는 번호 공백을 오류로 보지 않는다

`validate_pairs` 는 **실제 존재하는 tar 만** `(crop, split, number)` 키로 묶는다.

```python
grouped = defaultdict(dict)
for a in archives:
    grouped[(a.crop_folder, a.split, a.number)][a.role] = a
```

4·5번 tar 가 없으면 그 키 자체가 만들어지지 않아 검사 대상에서 빠진다. 요구사항의 "실제 존재하는 tar 번호를 기준으로 짝 검사"는 **이미 그렇게 동작하고 있다.**

현재 데이터 전수 조사:

| crop/split | labeled 번호 | source 번호 | 짝 성립 | 번호 공백 |
|---|---|---|---|---|
| chard/training | [1] | [1] | 1 | 없음 |
| **chard/validation** | **[2]** | **[1]** | **0** | **없음** |
| lettuce/training | 1~75 | 1~75 | 75 | 없음 |
| lettuce/validation | 1~10 | 1~10 | 10 | 없음 |

**번호 공백은 한 건도 없다.** 실제 오류는 `chard/validation` 하나뿐이며, 이는 번호 공백이 아니라 VL#1 과 VS#2 가 없는 상태다. 두 tar 내부를 확인한 결과:

```
VL_2.근대2.tar  JSON 500건   샘플 stem: C26_L04_04_005_755935
VS_2.근대1.tar  JPG  500건   샘플 stem: C26_L04_01_001_245943
stem 교집합: 0건 (0.0%)
```

서로 다른 개체 데이터다. 번호만 어긋난 같은 짝이 아니라 진짜 결손이므로, 요구사항의 오류 기준("특정 번호에 labeled만 / source만 있는 경우")에 정확히 해당한다. **오류가 맞다.**

다만 현재 메시지가 오해를 부른다.

```
[!!] chard/validation #2 — labeled VL_2.근대2 의 source 짝 없음
[!!] chard/validation #1 — source VS_2.근대1 의 labeled 짝 없음
```

"짝 없음"이 번호가 비어서인지 파일이 결손인지 구분되지 않고, 이 하드 오류 하나 때문에 `--skip-broken` 없이는 lettuce 85쌍까지 전부 중단된다(exit 2).

### B-2. 할 일

이미 되어 있는 것을 다시 만들지 않고, 요구사항의 취지(오해 방지)를 만족시킨다.

1. **번호 공백 감지 → 정보성 로그 신규 추가** (현재는 감지 자체를 하지 않음)

   ```
   [정보] lettuce/training — 아카이브 번호 공백 감지: 4, 5
          실제 존재하는 TL/TS 짝을 기준으로 계속 진행합니다.
   ```

   `(crop, split)` 별로 존재하는 번호의 min~max 범위에서 **양쪽 다 없는 번호**만 공백으로 집계한다. 한쪽만 있는 번호는 공백이 아니라 오류로 분류한다.

2. **오류 메시지에 "번호 공백이 아님"을 명시**하고 판단 근거를 함께 출력

   ```
   [!!] chard/validation #2 — labeled VL_2.근대2 는 있는데 source VS_2.근대2 가 없습니다
        (번호 공백이 아니라 한쪽 tar 결손입니다. 해당 번호의 source tar 를 받으세요)
        이 split 의 번호 구성 — labeled: [2] · source: [1]
   ```

3. **검증 요약에서 공백/경고/오류를 분리 집계**

   ```
   [진행] 아카이브 검증 완료 — 사용 가능 85쌍 · 번호 공백 0 · 경고 0 · 오류 2
   ```

4. **`ARCHIVE_RE` 불일치로 건너뛴 파일을 검증 요약에 모아 출력**. 현재는 파일마다 흩어져 나오는데, 한쪽만 스킵되면 "짝 없음" 오류로 번져 번호 공백처럼 보이는 진짜 원인이 될 수 있다.

**짝 판정 로직 자체는 변경하지 않는다.** 감지·분류·문구만 추가한다.

---

## 이슈 C — GUI 기본값 초기화

### C-1. 기본값 정의 (단일 소스)

`_build_vars()` 에 리터럴로 흩어져 있는 초기값을 모듈 상수 `DEFAULTS` 로 뽑고, `_build_vars()` 가 이를 읽어 변수를 만든다. 초기화 버튼도 같은 딕셔너리를 쓴다. → **"GUI 시작 상태 == 초기화 결과"가 코드로 보장된다.**

값은 현재 초기값을 그대로 옮기기만 한다(기본 동작 불변).

### C-2. 버튼

`④ 실행` 카드의 `■ 중단` 버튼 아래에 `↺ 기본값 초기화` 배치. 실행 영역이라 발견하기 쉽고, 사이드바 최상단보다 오조작 위험이 낮다.

### C-3. 초기화 대상 / 비대상

| 초기화함 | 건드리지 않음 |
|---|---|
| 데이터·모델·학습·고급 설정 전체 (`self.v`) | `runs/` 디렉터리, 학습 결과, 모델 파일, 데이터 파일 |
| 가설 입력란 → 비움 | 로그 창 내용 |
| 결론 입력란 → 비움 | 이미 로드된 run 의 지표 카드·곡선·혼동행렬 화면 |
| Study 이름·비교 모델 선택·LR 자동 탐색 → 기본값 | 실행 중인 프로세스 |
| 실행 모드 → `단일 실행` | 기준 run 의 파일 |
| 기준 run 선택 → `(없음)` | |

Study 설정과 실행 모드를 포함하는 이유: 이슈 작성 시점에는 Study 모드가 없었으나, 지금은 이것도 "편집 중인 설정"이다.
기준 run 선택을 되돌리는 이유: 기준 run 은 `config.json` 의 `parent_run` 필드로 들어가므로, 완료 조건인 "초기화 후 새 실험의 config.json 에 기본값 저장"을 만족시키려면 `(없음)` 이어야 한다. **파일은 삭제하지 않는다.**

### C-4. 동작

1. 실행 중(`self.proc` 살아 있음)이면 버튼 **비활성**. 단일/Study 시작 시 disable, 종료 시 enable (기존 `run_btn`/`stop_btn` 토글 지점에서 함께 처리)
2. 클릭 → 확인 대화상자

   ```
   현재 편집 중인 설정을 기본값으로 되돌릴까요?

   생성된 실험 결과와 파일은 삭제되지 않습니다.
   ```

3. 확인 시 초기화 → 로그에 `[완료] GUI 설정을 기본값으로 초기화했습니다.`
4. `_on_lr()`, `_on_arch()`, `_on_mode()`, `_on_pick()`, `_estimate()` 재호출로 표시 갱신(LR 라벨, simple_cnn 프레임, 예상 소요 등)

---

## 검증 계획

### 단위 테스트 (`tests/test_prepare_dataset.py`)

- 진행률 스로틀: 0.5초 미만 연속 호출은 출력하지 않음 / 0%·100% 는 항상 출력
- TTY·비TTY 분기: 비TTY 에서 `\r` 미사용
- ETA·시간 포맷: 0건, 1건, 0 나눗셈 방어
- 경고 출력 시 진행 줄을 지우고 찍는지
- 번호 공백 감지: `[1,2,3,6,7,8]` → 공백 `[4,5]`, 오류 0건
- 한쪽만 있는 번호: `labeled=[1,2], source=[1]` → 공백 0, 오류 1건
- 공백과 결손이 동시에 있을 때 분리 집계
- 기존 `test_reconcile_extracted_pair_deletes_unpaired_files` 유지

### 통합 테스트 — 합성 tar (실데이터 미사용)

스크래치패드에 소형 tar 쌍(아카이브당 30~50파일, 유효 JSON + 실제 JPEG)을 만들어 `build_index` 를 끝까지 실행한다.

- 번호 `1,2,3,6,7` 구성 → 오류 0건 · `[정보] 번호 공백: 4, 5` 출력 · index.csv 정상 생성
- **진행 표시 추가 전/후 index.csv 바이트 동일** ← 회귀의 핵심
- 파이프 리다이렉트 시 `\r` 없이 읽히는지 + `—` 문자 크래시 없는지
- 깨진 JSON·손상 JPG 를 넣어 경고가 진행 줄을 망가뜨리지 않는지
- 미지 라벨을 넣어 중단 메시지에 아카이브·단계가 찍히는지

### 실데이터 — 읽기 전용 검증만

- `validate_pairs` 만 실행해 chard 오류 2건 + lettuce 85쌍 정상 + 공백 0건이 새 문구로 나오는지 확인
- **`data/index.csv`(현재 5,000행)는 덮어쓰지 않는다.** 전체 재생성은 42.3GB 추출 + 약 43,500행 변경이므로 별도 승인이 필요하다.

### GUI 스모크 (기존 57항목에 추가)

- 초기화 후 모든 `self.v` 값이 `DEFAULTS` 와 일치
- 가설·결론 입력란 비워짐, 모드가 `단일 실행`, 기준 run `(없음)`
- 실행 중 버튼 비활성 / 종료 후 활성
- 초기화 직후 `build_config()` 결과가 기본값과 일치 (완료 조건의 "config.json 에 기본값 정상 저장")
- **`runs/` 디렉터리 파일 수가 초기화 전후 동일**

---

## 결정 사항

| 항목 | 결정 |
|---|---|
| ETA(경과/예상 잔여) 표시 | 포함 |
| 스피너 | TTY 일 때만 기본 ON, `--no-spinner` 로 해제 |
| UTF-8 출력 고정 | 이번 작업에 포함 |
| Study 설정·실행 모드 초기화 | 포함 |
| 기준 run 선택 초기화 | `(없음)` 으로 복귀, 파일은 보존 |
| 초기화 버튼 위치 | `④ 실행` 카드의 중단 버튼 아래 |
| 커밋 분리 | 이번에는 하지 않음 (나중에 진행) |

## 구현 순서

A(진행 표시) → B(번호 공백) → C(GUI 초기화)
