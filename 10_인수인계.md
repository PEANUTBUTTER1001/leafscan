# 인수인계 — GUI Study 실행 모드 · 전처리 진행 표시 · GUI 기본값 초기화

> 작성일: 2026-08-13
> 기준 문서: `06_GUI_Study_실행모드_SRS.md`, `07_GUI_Study_MVP_기획서.md`,
> `08_GUI_Study_MVP_실행명세서.md`, `09_전처리_진행표시_및_GUI_초기화_구현계획.md`
> 상태: **구현 완료 · 커밋 전** (브랜치 `develop`)

---

## 1. 한 줄 요약

GUI 에 Study(다중 모델 순차 비교) 실행 모드와 기본값 초기화를 추가하고,
`prepare_dataset.py` 에 진행 표시를 넣었다. 그 과정에서 발견한 결함 8건을 함께 고쳤다.
**미해결 항목 5건이 남아 있으며, 그중 U-1(NaN 발산 미감지)은 잘못된 실험 결과를
정상으로 보고하므로 우선 처리를 권한다.**

---

## 2. 변경 파일

| 파일 | 상태 | 내용 |
|---|---|---|
| `lab_gui.py` | 수정 | 프리셋 제거, Study 실행 모드, 기본값 초기화, 혼동행렬 탭 배치, 자식 인터프리터 고정 |
| `core/study_link.py` | **신규** | GUI ↔ `run_study.py` 브리지 (로그 파싱·config 변환). **tkinter 비의존** |
| `core/figures_study.py` | 수정 | `runs_parallel` y축 범위 고정 1줄 |
| `scripts/prepare_dataset.py` | 수정 | 진행 표시, 번호 공백 안내, UTF-8 출력 고정 |
| `tests/test_gui_study.py` | **신규** | Study 브리지 로직 10건 |
| `tests/test_prepare_dataset.py` | 수정 | 1건 → 16건 |
| `06~09_*.md` | 신규 | 기획·명세·구현 계획 |

**수정하지 않은 파일**: `run_study.py`, `train_worker.py`, `scripts/make_study.py`,
`core/data.py`, `core/split.py`, `tasks/models_registry.py` — 학습·분할·비교 규칙은 그대로 재사용한다.

---

## 3. 구현한 기능

### 3.1 GUI Study 실행 모드

- 사이드바 최상단 `실행 모드: 단일 실행 / Study 실행` (기본 단일 실행)
- Study 설정: 이름, 비교 모델 체크박스(최소 2개), 모델별 LR 자동 탐색, 큐 상태, Study 폴더 열기
- 실행 버튼이 모드에 따라 분기 (`start_run()` → `start_training()` / `start_study()`)
- `run_study.py --study --base --vary arch=a,b,c [--lr-search]` 단일 프로세스로 순차 실행
- `run_study.py` 표준 출력을 파싱해 큐 상태 표시 (`대기/실행 중/LR 탐색/완료/건너뜀/실패`)
- 모델이 바뀌면 손실 곡선 리셋, 모델 완료 시 해당 member 를 `_load_run()` 으로 즉시 표시
- 종료 후 `scripts/make_study.py` 로 비교 그림 4종 생성 → 최종 완료 블록 출력
- 중단은 프로세스 **트리** 종료(`taskkill /F /T`), 재개는 `run_study.py` 기존 규칙 사용

### 3.2 모드별 UI

쓸 수 없는 항목은 회색 처리가 아니라 **숨기고** 그 자리에 이유를 남긴다(사용자 피드백).

| 항목 | 단일 | Study |
|---|---|---|
| `arch` 콤보 행 | 표시 | 숨김 + 안내 |
| simple_cnn 전용 프레임 | arch=simple_cnn 일 때 | 숨김 |
| 가설 라벨·입력 | 표시 | 숨김 + 안내 |
| Study 설정 프레임 | 숨김 | 표시 |
| `결론 저장`·`report.md 열기` | 표시 | 숨김 |
| `STUDY.md 열기` | 숨김 | 표시 |
| 결론 입력칸 | 표시 | 숨김(로그 영역 확장) |

### 3.3 GUI 기본값 초기화

- 모듈 상수 `DEFAULTS` 하나를 `_build_vars()` 와 초기화가 공유 → "시작 상태 == 초기화 결과" 보장
- `④ 실행` 카드의 중단 버튼 아래 `↺ 기본값 초기화`
- 초기화 대상: 전체 설정, 가설·결론 입력, Study 설정, 실행 모드, 기준 run 선택
- **삭제하지 않는 것**: `runs/`, 학습 결과, 모델·데이터 파일
- 실행 중 버튼 비활성 + 함수 진입 차단(이중 방어)

### 3.4 `prepare_dataset.py` 진행 표시

- `[단계 1/5]` ~ `[단계 5/5]` 헤더
- 아카이브별 진행률 + 등록/제외 + 경과/남은 예상 시간
- **TTY**: `\r` 제자리 갱신 + 스피너 / **비TTY**: `\r` 없이 새 줄(로그 파일 보호)
- `--quiet`, `--progress-interval`, `--no-spinner` 추가
- 중단 메시지에 아카이브·단계·진행 상황 포함

### 3.5 아카이브 번호 공백

- `number_gaps()` 신규 — 양쪽 role 이 모두 없는 번호만 "공백"으로 보고 **정보성 로그**로 출력
- 한쪽만 있는 번호는 공백이 아니라 결손 → 오류. 메시지에 "번호 공백이 아니라 한쪽 tar 결손"과
  해당 split 의 번호 구성을 함께 출력해 오해를 막는다
- 검증 요약에 `번호 공백 N · 경고 N · 오류 N` 분리 집계

> 참고: "중간 번호가 비면 오류가 난다"는 원 증상은 **현재 코드에서 재현되지 않았다.**
> `validate_pairs` 는 존재하는 번호만 키로 묶기 때문이다. 위 작업은 오해 방지가 목적이다.

---

## 4. 해결한 결함 (문제 해결 사항)

### D-1. pythonw 로 실행 시 학습 로그 유실 + 가중치 다운로드 크래시 ★ 영향 큼

- **증상**: Study 실행 시 `efficientnet_b0`, `resnet50` 이 `'NoneType' object has no attribute 'write'`
  로 실패. 학습 로그·진행률·손실 곡선이 GUI 에 하나도 오지 않음
- **원인**: `LeafScan Lab.bat` 이 `pythonw.exe` 로 GUI 를 띄우고, `sys.executable` 이 자식 체인
  전체로 전파된다. pythonw 로 실행된 프로세스가 표준 핸들을 **명시하지 않고 상속만** 시키면
  손자 프로세스의 `sys.stdout/stderr` 가 `None` 이 된다.

  ```
  GUI(pythonw)                    stdout 유효
   └ run_study.py (PIPE 명시)      stdout 유효   ← run_study 로그만 보였던 이유
      └ train_worker.py (상속)      stdout=None, stderr=None   ← 여기서 깨짐
  ```

  → `print()` 는 조용한 no-op 이 되고, torchvision 이 사전학습 가중치를 받을 때 tqdm 이
  `sys.stderr` 에 쓰다가 죽는다. **토치 캐시에 없던 모델만 실패**한 이유가 이것이다.
- **수정**: `lab_gui.PYTHON` 상수 — pythonw 로 실행 중이면 같은 폴더의 `python.exe` 로 교체.
  `CREATE_NO_WINDOW` 를 함께 주므로 콘솔 창은 뜨지 않는다. 모든 자식 프로세스에 적용
- **검증**: pythonw 에서 실제 코드 경로로 `efficientnet_b0`(다운로드 필요) 학습 완주 확인

### D-2. Study 로그 파일에 완료 요약이 통째로 누락

- `_on_study_exit()` 가 요약을 쓰기 전에 `log_fp` 를 닫고 있었다. GUI 창에는 보이지만
  `runs/<study>/_gui_study.log` 에는 남지 않았다
- 비교 그림 생성까지 파일을 열어두고, **맨 마지막에 완료 블록**(상태·집계·소요 시간·경로)을
  찍은 뒤 닫도록 변경

### D-3. `[모델 완료]` 가 다음 모델의 `[실행]` 뒤에 찍힘 + 태그 충돌

- `_poll()` 이 원문 로그를 먼저 출력한 뒤 파서를 호출해 순서가 뒤집혔다
- 파서를 원문 출력보다 먼저 호출하도록 변경. 멤버 완료 태그를 `[모델 완료]` 로 분리해
  `run_study.py` 의 `[완료] N개 run`(study 전체)과 구분

### D-4. `runs_parallel.png` 가 4494px 짜리 이미지로 생성

- 모든 축이 상수면 정규화 값이 전부 0 → y 범위가 ±0.05 로 축소 → min/max 주석(y=-0.06, 1.02)이
  축 밖으로 나가고 `savefig(bbox="tight")` 가 캔버스를 그만큼 늘린다
- `core/figures_study.py` 의 `runs_parallel()` 에 `ax.set_ylim(-0.12, 1.12)` 추가 → 794×564 로 정상화

### D-5. 혼동행렬이 head 탭 **밖**에 렌더링

- 탭 프레임이 비어 있고 캔버스는 Notebook 의 형제로 아래에 packed 되어 있었다
- 탭마다 자기 `ConfusionMatrix` 를 소유하도록 변경(`_add_head_tab` / `_reset_head_tabs`).
  탭 전환 시 처음 볼 때만 배열 로드

### D-6. 재개 시 설정 변경을 감지하지 않아 비교표 오염

- 재개 판정은 `metrics.json` 유무만 본다. 그 사이 설정이 바뀌면 **서로 다른 조건으로 학습된
  run 이 한 STUDY.md 표에 섞인다** (실제로 `study_01_backbone` 에서 발생 — §5 U-3)
- `study_config_diff()` 신규 + 시작 전 확인 대화상자. `arch`·`hypothesis`·`study` 등
  run 마다 달라지는 키는 비교에서 제외

### D-7. pytest 에서 tkinter + torch 동시 로드 시 Windows DLL 접근위반

- `tests/test_gui_study.py` 가 `lab_gui`(→tkinter)를 import 하면, 알파벳 순으로 나중에 수집되는
  `test_matrix.py`(→torch)가 로드될 때 `Windows fatal exception: access violation` 덤프 발생
- 순수 로직을 **tkinter 비의존** `core/study_link.py` 로 분리하고 테스트는 그 모듈만 import
- **GUI 위젯 동작은 pytest 가 아니라 별도 스모크 스크립트로 검증한다** (창 생성 → update → destroy)

### D-8. `prepare_dataset.py` 출력을 리다이렉트하면 크래시

- 로그 문구의 `—`(em dash)가 cp949 로 인코딩되지 않아 `UnicodeEncodeError`.
  터미널에서는 멀쩡하고 `> log.txt` 에서만 죽는다
- 스크립트 시작에서 stdout/stderr 를 UTF-8 로 재설정

---

## 5. 미해결 항목 (다음 담당자 판단 필요)

### U-1. NaN 발산을 아무도 감지하지 않음 ★ 우선 처리 권장

- `runs/study_01_backbone/study_01_backbone__arch-resnet18` 의 로짓이 **100% NaN**(3377/3377).
  `argmax` 가 인덱스 0 을 돌려줘 stage 예측이 전부 `정식기` 로 몰린다
- 학습 이력이 1 epoch 부터 `loss=nan, val_loss=nan`. **같은 split·같은 설정의 `convnext_tiny` 는
  정상**(val_acc 0.89)이므로 arch 특이 문제(AMP fp16 오버플로 의심)
- `train_worker.py` / `core/metrics.py` / `core/losses.py` 전체에 `isnan`·`isfinite`·`diverged`
  검색 결과 **0건**. `core/config.py` 주석은 `failure_kind: diverged` 를 언급하지만 설정하는 코드가 없다
- 그 결과 NaN run 이 `metrics.json` 과 함께 저장되고 → `run_study.py` 가 성공 집계 →
  GUI 가 `✔ 완료` 표시 → STUDY.md 에 `macro-F1 0.1449 · 정식기 recall 1.0` 이 정상 수치처럼 등재된다.
  **조용히 틀린 결과가 실패보다 나쁘다**
- 제안: (A) `train_worker.py` epoch 루프에 NaN/inf 가드 → `failure_kind="diverged"` 로 실패 처리,
  (B) GUI 가 `history` 의 NaN 을 감지해 `✖ 발산` 으로 표시. **A+B 권장**
- 임시 회피: `epochs > freeze_epochs` 로 설정, LR 하향(1e-3→3e-4), 또는 config 에 `"amp": false`

### U-2. 원본 데이터 짝 불량 34건

읽기 전용 전수 검사 결과:

```
아카이브 174개 · 명명 불일치 0 · 번호 공백 0
사용 가능 52쌍 / 전체 86쌍
오류 34: zero_intersection 32 · missing_labeled 1 · missing_source 1
경고 6: low_intersection
```

- `lettuce/training` **41~74번 중 32쌍**이 TL/TS 내부 stem 교집합 0건 — 번호는 맞는데 내용물이 다르다
- `chard/validation` 은 VL#2 / VS#1 만 존재 (교집합 0 확인)
- 코드 문제가 아니라 원본 재다운로드가 필요한 사안. 현 상태로 전체 인제스트 시 86쌍 중 52쌍만 사용 가능

### U-3. `runs/study_01_backbone` 에 서로 다른 조건의 run 혼재

| 멤버 | dataset | img | heads |
|---|---|---|---|
| simple_cnn | **fake** | 224 | `label` |
| mobilenet_v3_small | **fake** | 224 | `label` |
| resnet18 | index_csv | 192 | crop, stage (**NaN**) |
| convnext_tiny | index_csv | 192 | crop, stage |

- 초기 더미 테스트 잔재가 재개 규칙으로 건너뛰어져 실데이터 run 과 한 표에 섞였다(D-6 의 실제 사례)
- 조치안: ① 더미 멤버 2개 + NaN resnet18 삭제 후 재실행, ② 새 Study 이름으로 재시작, ③ 유지
- **파일 삭제는 하지 않았다.** 판단 후 처리 필요

### U-4. `scripts/make_study.py` 수동 실행 시 cp949 크래시

- 결과 출력의 `✓` 문자에서 `UnicodeEncodeError`. **그림 파일은 정상 생성된 뒤** 출력 단계에서만 죽는다
- GUI 는 `PYTHONUTF8=1` 을 넘기므로 영향 없음. 수동 실행은 `python -X utf8 scripts/make_study.py ...`
- D-8 과 같은 부류. 미수정(이번 범위 밖)

### U-5. 커밋 미실시

- 작업 전체가 워킹트리에만 있다. 브랜치 `develop`, 원격 `origin/develop`
- 기능·수정·문서가 섞여 있어 커밋 분리 권장 (§7)

---

## 6. 검증 상태

| 항목 | 결과 |
|---|---|
| `python -m pytest tests/ -q` | **30 passed** (기존 5 → 30) |
| GUI 스모크 스크립트 | **79/79 OK** (모드 전환·숨김·큐 파싱·초기화·잠금) |
| Study e2e (`multilabel_fake` 2모델) | 순차 실행·STUDY.md·비교 그림 4종·재개 건너뜀 확인 |
| 진행 표시 회귀 | 표시 ON/OFF 두 실행의 `index.csv` **바이트 동일** |
| 비TTY 로그 | 잔여 `\r` 없음 (CR 수 == LF 수) |
| pythonw 체인 | 수정 전 `stdout=None` → 수정 후 정상, 다운로드 포함 학습 완주 |
| `data/index.csv` | **무변경** (1,013,279 bytes · 5,000행) |

**미검증**

- 실사용 중단 시 프로세스 트리 종료 (`tasklist` 로 잔존 프로세스 확인 필요)
- 실데이터 전체 인제스트 (42.3GB 추출 + 약 43,500행 생성 — 별도 승인 필요)
- LR 자동 탐색(`--lr-search`) 경로
- 3개 이상 모델 동시 Study

---

## 7. 다음 담당자를 위한 메모

1. **`train_worker.py` 를 pythonw 로 띄우지 말 것.** `lab_gui.PYTHON` 을 우회해 `sys.executable`
   을 직접 쓰면 D-1 이 재발한다
2. **GUI 로직 테스트는 `core/study_link.py` 에 둘 것.** `lab_gui` 를 import 하는 테스트는 D-7 을 부른다
3. **`run_study.py` 의 로그 문구를 바꾸면** `tests/test_gui_study.py` 가 먼저 깨진다. 의도된 안전장치다
4. `runs/<study>/_gui_base.json` 은 GUI 가 생성하는 Study base config 다. `_` 로 시작해
   `collect_members()` 의 비교 대상에서 자동 제외된다. **Study 이름을 `_` 로 시작할 수 없는 이유**
5. `--lr-search` 를 쓰면 run 폴더명이 `…__arch-X__lr-0.001` 이 된다. arch 파싱은 `__` 앞까지만 취한다
6. 진행 표시의 `Reporter` 는 `stream`·`clock` 주입이 가능하다. 테스트는 이걸로 결정론적으로 검증한다
