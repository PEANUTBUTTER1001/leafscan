# LeafScan 기획서 — 엽채류 판별 모델 연구

> **프로젝트 성격**: 딥러닝 모델 **연구·학습** 프로젝트. 스마트폰 앱은 포함하지 않는다.
> 학습된 모델은 표준 번들 형식(`make_bundle.py`)으로 내보내는 것까지가 이 프로젝트의 범위다.

---

## 1. 개요

| 항목 | 내용 |
|---|---|
| 프로젝트명 | LeafScan (가칭) |
| 목적 | 엽채류 이미지에서 **작물 종류**와 **생육단계**를 판별하는 모델을 연구·개발 |
| 대상 작물 | 상추 · 케일 · 겨자채 · 근대 (4종) |
| 생육단계 | 정식기 · 생육기 · 수확기 (3단계) |
| 문제 유형 | 이미지 분류 — **백본 공유 멀티헤드** (crop 4 + stage 3) |
| 프레임워크 | PyTorch + torchvision |
| 참여 | 팀원 다수 — 각자 PC에서 실험 후 결과를 합침 |
| 산출물 | 학습된 모델 · 연구 기록 · 시각 자료 · 모델 번들 |

### 1.1 이 프로젝트가 아닌 것

| 아닌 것 | 이유 |
|---|---|
| 안드로이드 앱 개발 | 범위 밖 — 별도 프로젝트가 담당 |
| 모델 경량화·변환의 **주** 목적 | 변환은 인계 시점의 한 단계일 뿐. 크기 제약을 두지 않는다 |
| 병해충 진단 · 수확 시기 예측 | 별도 데이터·문제 정의가 필요 |
| 객체 탐지 · 이미지 분할 | 확장점만 열어두고 착수하지 않음 |

> **이전 문서에 있던 "모델 10MB 이하" 제약은 폐기되었다.** LeafScan은 크기·속도 제약 없이 **정확도와 연구 품질**을 추구한다. 배포 관련 판단은 인계 이후 하류 소비자 측에서 실측을 근거로 한다.

---

## 2. 목표 — 세 축

이 프로젝트의 산출물은 "정확한 모델 하나"가 아니라 **연구 과정 전체**다.

| 축 | 목표 | 판정 기준 |
|---|---|---|
| **A. 재사용 가능한 뼈대** | 과제가 바뀌어도 살아남는 학습 하네스. 모델 교체가 config 한 줄 | 코드를 열지 않고 모델을 바꿀 수 있는가 |
| **B. 연구 기록** | 어떤 모델·설정을 만져 정확도를 어떻게 올렸는지가 남는다 | 3주 뒤에 이야기를 재구성할 수 있는가 |
| **C. 시각 자료 수집** | 연구 과정에서 보여줄 수 있는 그림이 자동 축적된다 | 자료를 만들 때 스크린샷을 찍어야 하는가 |

핵심 설계를 한 문장으로:

> **run이 끝날 때 `report.md`와 `figures/`가 자동으로 생기고, 모델은 config 한 줄로 바뀐다.**

---

## 3. 데이터

> **상세 규격은 `05_데이터_명세서.md`에 있다.** 여기서는 기획 판단에 필요한 요약만 다룬다.

### 3.1 원본 데이터셋

| 항목 | 내용 |
|---|---|
| 제목 | 수직농장 통합데이터 — 엽채류 |
| 배치 | `data/{작물영문}/{training\|validation}/{labeled\|source}/*.tar` |
| 작물 폴더 | `lettuce`(상추) · `chard`(근대) · `kale`(케일) · `leaf_mustard`(겨자채) |
| 형식 | 이미지 tar + 어노테이션 JSON tar (이미지 1장당 JSON 1개) |
| 규모 | 수십 GB — 아카이브 1개당 177~500장 |
| Git | **커밋하지 않음** (`04_팀_협업_규약.md`) |

### 3.2 사용 범위

| 데이터 | 사용 | 사유 |
|---|---|---|
| **이미지 + `crops` + `growth_stage`** | ✅ 핵심 학습 데이터 | 라벨이 이미 존재 → 별도 라벨링 불필요 |
| `crops_id` + 개체번호 | ✅ **group_id** | 데이터 누수 방지에 필수 |
| `kind_type` (품종) | △ 분석 축 | 아카이브 단위로 고정 → 그룹과 교락 |
| `bbox` (plant_body) | △ **전처리 옵션** | 분류에 쓰지 않지만 **입력 crop 방식의 실험 변수**로 활용 (명세서 §7) |
| 재배환경 데이터 | ❌ | 앱에서 입력 불가 — 이미지만으로 판별하는 것이 문제 정의 |
| 생육지표 데이터 | ❌ | 샘플에서 전부 `null` |

### 3.3 실측으로 드러난 데이터 특성 — 네 가지 난제

| # | 난제 | 내용 | 대응 |
|---|---|---|---|
| **N1** | **클래스 불균형** | 샘플 기준 생육기 73% / 정식기 20% / 수확기 6% — **아카이브마다 편차가 극심** | 층화 분할 · class weight · 차등 augmentation. 평가는 **macro-F1과 정식기 recall** 기준 |
| **N2** | **개체 단위 시계열** | 한 개체를 **약 한 달간 27~40장 연속 촬영** → 이미지들이 매우 유사 | **그룹 분할 필수.** 무작위 분할 시 정확도 99% 같은 헛된 수치가 나온다 |
| **N3** | **아카이브별 단계 편중** | 한 아카이브에 **한 단계만** 있는 경우가 실제로 존재 | 아카이브 단위 분할 금지. **`StratifiedGroupKFold`** 로 층화와 그룹을 동시에 만족 |
| **N4** | **생육정지 현상** | 정식기와 생육기의 외형이 겹치는 구간 — 이미지만으로 원리적으로 구분 불가 | `ambiguous` 태그를 오분류 분석으로 사후 부여, 정책을 실험 변수로 |

> N4는 **없앨 수 없다.** 인정하고 다루는 것이 이 프로젝트의 연구적 성격이다. "정확도가 왜 100%가 안 되는가"의 답이 여기 있고, 이를 데이터로 규명하는 것 자체가 결과물이다.
>
> N2·N3은 초보자가 가장 자주 놓치는 함정이다. **분할을 잘못하면 그 뒤의 모든 실험이 무의미해진다.**

### 3.4 데이터 준비 산출물

| 산출물 | 내용 |
|---|---|
| `data/index.csv` | 13개 컬럼 — 학습이 읽는 **유일한** 데이터 정의 (명세서 §6.5) |
| `data/labels.json` | 라벨 매핑 + 전처리 규격 (앱 인계 시 그대로 사용) |
| `data/MANIFEST.md` | 아카이브 목록·건수·해시·제외 건수 |
| `data/_extracted/` | tar 추출본 (Git 제외) |

> **인제스트는 아카이브 짝 검증부터 시작한다.** `labeled`와 `source`의 묶음 번호가 다르면 교집합이 0건이 되어 학습이 통째로 무의미해진다. 실제로 현재 샘플 데이터가 이 상태다 (명세서 §1.1).

---

## 4. 모델 전략

### 4.1 비교 대상 — 5종 + 픽스처 1종

**"최신 모델 하나"가 아니라 문제 유형별로 검증된 모델을 같은 조건에서 비교**하는 것이 목표다.

| key | 계열 | 파라미터 | 역할 |
|---|---|---|---|
| `resnet50` | CNN 표준 | 25.6M | **베이스라인** — 자료가 가장 많고 구조가 표준적 |
| `efficientnet_b0` | CNN 효율 | 5.3M | 연산 대비 성능 |
| `convnext_tiny` | CNN (Transformer 아이디어 반영) | 28.6M | 최신 계열 대표 |
| `resnet18` | CNN 경량 | 11.7M | 빠른 반복·비교 하한 |
| `mobilenet_v3_small` | CNN 초경량 | 2.5M | 속도·크기 하한 |
| `simple_cnn` | 직접 구현 | ~0.1M | **테스트 픽스처** — 하네스 회귀 검증용, 실험 대상 아님 |

**ViT-B/16은 제외한다.** 팀원 PC의 VRAM(4~8GB) 편차에서 안정적으로 학습하기 어렵고, 데이터 규모 대비 이득이 불확실하며, ConvNeXt-Tiny가 Transformer 계열 아이디어를 CNN으로 흡수한 모델이라 계열 다양성은 이미 확보된다. *(추후 여력이 생기면 `efficientnet_b2`를 먼저 추가하는 편이 비용 대비 효과가 낫다.)*

### 4.2 모델 구조 — 백본 공유 멀티헤드

작물 분류와 생육단계 분류는 **같은 이미지에서 나오는 두 개의 질문**이다. 백본 하나를 공유하고 head만 분리한다.

```
입력 224×224 → [백본 (교체 가능)] → feature → ┬→ crop_head  (Linear → 4)
                                              └→ stage_head (Linear → 3)

loss = w_crop · CE(crop) + w_stage · CE(stage, class_weight)
```

| 항목 | 결정 | 이유 |
|---|---|---|
| head 출력 | **logit만 반환** (softmax 없음) | `CrossEntropyLoss`가 내부 처리. 중복 적용 시 학습이 망가짐 |
| 손실 가중치 | 초기 `0.4 : 0.6` (stage 우위), 실험 변수 | 작물이 상대적으로 쉽게 수렴 |
| class weight | stage head에만 적용 | 불균형은 생육단계 쪽 문제 |
| 단일 head | `heads={"label": N}`인 특수 케이스로 통일 | 분기를 두 벌 만들면 한쪽만 고치는 버그가 생김 |

### 4.3 공정 비교 프로토콜

비교가 의미를 가지려면 **모델 외의 모든 조건이 같아야 한다.** 이를 사람의 성실성이 아니라 코드로 강제한다.

| 구분 | 항목 |
|---|---|
| **완전 고정** | 데이터 인덱스(해시 검증) · split seed · train/val/test 경계 · augmentation · class weight 방식 · 손실 가중치 · epoch 예산 · early stopping 규칙 · 평가 세트 |
| **모델별 허용** | arch · 정규화 상수 · batch size(VRAM) · **learning rate** |
| **금지** | 특정 모델만 augmentation 추가 · 특정 모델만 epoch 연장 · test set을 보고 선택 |

**learning rate를 고정하지 않는 이유** — ResNet-50과 ConvNeXt의 최적 lr은 자릿수가 다르다. 하나로 고정하면 "그 lr에 맞는 모델"이 이길 뿐이다. arch마다 3점 lr 탐색(예산 30%)을 먼저 돌리고 그 최적값으로 본 실험을 한다. **탐색 run도 전부 기록에 남아 공정성의 증거**가 된다.

### 4.4 정확도만 보지 않는다

비교 표에 비용 축을 함께 넣는다.

| 모델 | macro-F1 | 정식기 recall | 파라미터 | 학습 시간 | 추론 시간 | 메모리 |
|---|---|---|---|---|---|---|
| *(실험 후 채움)* | | | | | | |

---

## 5. 평가 지표

| 지표 | 용도 | 비고 |
|---|---|---|
| **stage macro-F1** | **주 지표** | 불균형 데이터에서 accuracy는 과대평가됨 |
| **정식기 recall** | **소수 클래스 검출력** | 이 프로젝트의 실질적 난이도 |
| crop accuracy | 작물 판별 | 비교적 쉬운 과제 |
| 클래스별 P/R/F1 | 약점 위치 파악 | head별 분리 평가 필수 |
| Confusion Matrix | 오분류 패턴 | 정식기↔생육기 경계 확인 |
| 미판정률 | threshold 정책의 비용 | 캘리브레이션 곡선과 함께 판단 |

> **accuracy를 주 지표로 쓰지 않는다.** 생육기가 60%이므로 전부 생육기로 찍어도 60%가 나온다.

---

## 6. 연구 계획 — study 단위

실험을 낱개로 굴리지 않고 **하나의 질문 = 하나의 study**로 묶는다. study 목록이 곧 연구 서사가 된다.

| study | 질문 | 예상 run | 산출 |
|---|---|---|---|
| **00** | 데이터가 어떻게 생겼나 | 분석만 | 12조합 분포표, `data_distribution.png`, **분할 정책 결정** |
| **01** | 어떤 백본이 이 문제에 맞나 | 15 + 5 | 공정 비교 표, `cost_accuracy.png` |
| **02** | 정식기 recall을 어떻게 올리나 | 4~6 | class weight · ambiguous 정책 결론 |
| **03** | 모델은 무엇을 보고 판단하나 | 분석만 | Grad-CAM 비교 (계열별 차이) |
| **04** | **입력을 자르면 좋아지나** (full / bbox / bbox_expand) | 3 | crop 방식 결론 → **앱 가이드 프레임 크기에 반영** |
| **05** | 해상도를 낮춰도 되나 | 3~4 | 속도·정확도 트레이드오프 |
| **06** | 전이학습 스케줄이 얼마나 기여하나 | 3 | freeze 구간 길이의 효과 |
| **07** | 양자화로 얼마나 잃나 | 2 | 인계용 모델 확정 |

> **study 00이 특히 중요하다.** 전체 아카이브를 모은 뒤 단계 분포와 개체 수를 봐야 분할 정책(`respect_provided` vs `resplit_all`)을 정할 수 있고, 그 결정이 이후 모든 실험의 전제가 된다.
>
> **study 04는 앱과 직접 연결된다.** bbox crop이 유리하다는 결과가 나오면, 앱의 촬영 가이드 프레임 크기를 그에 맞추는 것이 옳은 대응이다.

각 study는 `STUDY.md`로 결론이 남고, 전체는 `RESEARCH_LOG.md` 한 장에 모인다.

---

## 7. 산출물

| 산출물 | 내용 | 대상 |
|---|---|---|
| **학습 하네스** | config만으로 모델·데이터·손실을 바꿀 수 있는 코드 | 팀 내부 · 재사용 |
| **실험 콘솔** | Tkinter GUI — 값 조정·실행·결과 확인 | 팀 내부 |
| **run 기록** | run마다 `config·metrics·report.md·figures/` | 연구 기록 |
| **study 결론** | `STUDY.md` — 비교 표·그림·결론 | 연구 기록 |
| **`RESEARCH_LOG.md`** | 전체 연구 서사 인덱스 | **최종 문서 산출물** |
| **모델 번들** | 표준 번들 규격 (`make_bundle.py`) | 하류 소비자 |

---

## 8. 리스크

| 리스크 | 증상 | 대응 |
|---|---|---|
| 생육정지 구간 | 정식기↔생육기 오분류가 구조적으로 남음 | `ambiguous` 정책을 실험 변수로. **없앨 수 없음을 결과로 보고** |
| 클래스 편차 | 수확기·정식기 절대량 부족 | 층화 분할 필수, 부족 시 augmentation·가중치. 그래도 안 되면 데이터 보강 요청 |
| **데이터 누수** | 같은 개체가 train·val에 동시 존재 → **정확도 99% 같은 헛된 수치** | `group_id`(crops_id+개체번호) 기준 `StratifiedGroupKFold` |
| **아카이브 짝 오류** | labeled/source 번호 불일치 → 조인 결과 0건 | 인제스트 첫 단계에서 검증·중단 (명세서 §6.1) |
| **디스크 부족** | tar + 추출본으로 원본의 2배 필요 | 사전 안내, 작물별 단계적 추출 |
| **팀원 PC 편차** | GPU·CUDA·OS가 달라 결과가 미묘하게 다름 | 환경을 `env.json`에 기록, 비교 시 경고. `04_팀_협업_규약.md` |
| **실험 ID 충돌** | 두 사람이 동시에 `exp_006`을 만듦 | run ID에 작성자·날짜 포함 (규약 문서) |
| 실험 기록 유실 | 어느 설정에서 잘 나왔는지 놓침 | 가설·결론 필수 입력, 미작성 시 경고 |
| 도구가 본 과제를 잠식 | 하네스 만들다 연구가 밀림 | 계획서의 단계별 완료 판정을 지킴 |

---

## 9. 완료 기준

| 축 | 기준 |
|---|---|
| A | config만으로 5개 모델을 전환할 수 있고, 새 모델 추가가 레지스트리 한 항목으로 끝난다 |
| A | `tasks/` 파일 교체만으로 CIFAR-10과 LeafScan이 같은 하네스로 돌아간다 |
| B | 모든 run에 가설과 결론이 있고, `RESEARCH_LOG.md`만 읽고 연구 흐름을 이해할 수 있다 |
| B | study 01이 공정성 검사를 통과한 상태로 비교 결론에 도달한다 |
| C | run마다 그림 9종이 자동 생성되고, 자료 제작 시 스크린샷이 필요 없다 |
| — | 표준 번들 규격의 모델 번들이 생성되어 검증을 통과한다 |

---

## 관련 문서

| 문서 | 내용 |
|---|---|
| `02_구조도.md` | 시스템 구조·모델 레지스트리·산출물 규약 |
| `03_시작하기.md` | 설치·첫 실행·사용법·주의사항 |
| `04_팀_협업_규약.md` | 여러 PC에서 실험할 때의 규칙 |
| **`05_데이터_명세서.md`** | **데이터 구조·JSON 스키마·인제스트·분할 정책 (실측 기반)** |
